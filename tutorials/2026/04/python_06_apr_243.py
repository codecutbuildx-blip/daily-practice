# Segment Tree Implementation in Python
=====================================

A segment tree is a data structure used for range queries and updates on an array. It allows us to calculate the sum or product of any subarray in O(log n) time.